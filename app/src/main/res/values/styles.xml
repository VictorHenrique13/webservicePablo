<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="BottomSheetDialog" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/bottomSheetStyleWrapper</item>
    </style>

    <style name="bottomSheetStyleWrapper" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>
    <style name="dailogPerson" parent="Theme.AppCompat.Dialog.Alert">
        <item name="android:colorAccent" tools:ignore="NewApi">@color/colorPrimaryDark</item>
    </style>
    <style name="dialogCustom" parent="Theme.AppCompat.Dialog.Alert">
        <item name="colorAccent">@color/colorPrimaryDark</item>
        <item name="dialogCornerRadius">8dp</item>
        <item name="textColorAlertDialogListItem">@color/colorPrimaryDark</item>
        <item name="textAppearanceListItemSecondary">@color/colorPrimaryDark</item>
    </style>
</resources>
